# backend/render.yaml

services:
  - type: web
    name: image-collab-tool-api
    env: node
    buildCommand: cd backend && npm install && npx prisma generate
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
